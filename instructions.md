This might look familiar to yesterday's exercise, but this time we'll be using variables to solve the problem! 

The following is the formula for finding the area of a rectangle: 

```
area = height x width
```

1. Create a variable named `area.`

2. Use the `height` and `width` variables provided to calculate the area. (Note: You can change the value that's assigned into those variables by entering different values into the prompt when you click run. If no values are entered, your program will use the default ones provided — 150 and 230.)

3. Log the `area` variable to the console.